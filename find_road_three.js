import * as THREE from 'three';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import fs from 'fs';

// Let's run inside browser via CDP or a browser script to get the exact world positions of the road in the active Three.js scene!
